CONFIGURE=""
MAKEINSTALL=("cp -a arping $PREFIX/bin"
             "cp -a ping $EPREFIX/bin"
             "cp -a ping6 $EPREFIX/bin"
             "cp -a tracepath $PREFIX/sbin"
             "cp -a tracepath6 $PREFIX/sbin"
             "cp -a traceroute6 $PREFIX/sbin")
COMMENT="this will not take long"
