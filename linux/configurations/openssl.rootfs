ARCH="linux-$TARGET"
if [[ "$TARGET" =~ "i[3-6]86" ]]; then
    ARCH="linux-elf"
fi
if [[ "$TARGET" == "powerpc" ]]; then
    ARCH="linux-ppc"
fi
CONFIGURE="./Configure $ARCH --prefix=$USRROOT --openssldir=$USRROOT/lib/ssl shared"
MAKEINSTALL="make $MAKEOPTS install_sw"
COMMENT="about 2 minutes"
