CONFIGURE="./configure CFLAGS=-I$BUILDROOT/zlib --prefix=$ROOT --build=$HOST-linux --host=$TARGET-linux --disable-nls"
MAKEBUILD=("cd getopt"
           "make $MAKEOPTS"
           "cd ../utils"
	   "make $MAKEOPTS"
	   "cd ..")
MAKEINSTALL=("cd utils"
             "make $MAKEOPTS install"
	     "cd ..")
COMMENT="this will not take long"
