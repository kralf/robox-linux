CFLAGS="$CFLAGS -I$BUILDROOT/pcre"
CONFIGURE="./configure --prefix=$USRROOT --exec-prefix=$ROOT --host=$TARGET-linux --infodir=$ROOT/usr/share/info --disable-nls"
MAKEINSTALL=("rm -f $ROOT/bin/?grep"
             "make $MAKEOPTS install")
COMMENT="another quick build"
