CONFIGURE="./configure --prefix=$USRROOT --exec-prefix=$ROOT --host=$TARGET-linux CC_FOR_BUILD=cc CFLAGS_FOR_BUILD=-L$ROOT/lib --disable-nls"
MAKEINSTALL=("make $MAKEOPTS install"
             "rm -rf $ROOT/lib/pkgconfig")
COMMENT="a quick build"
