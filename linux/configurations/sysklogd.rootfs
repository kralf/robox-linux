CONFIGURE=""
MAKEBUILD="make $MAKEOPTS CC=$CC"
MAKEINSTALL=("mkdir -p $ROOT/usr/man/man5"
             "mkdir -p $ROOT/usr/man/man8"
             "make $MAKEOPTS BINDIR=$ROOT/sbin MANDIR=$USRROOT/man CC=$CC install")
COMMENT="won't take long"
